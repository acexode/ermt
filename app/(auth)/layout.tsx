'use client';
import { AuthLayout } from 'src/layouts/auth/layout';

export default function AuthLayoutWrapper({ children }: { children: React.ReactNode }) {
  return <AuthLayout sx={{}} cssVars={{}} children={children} />;
} 
